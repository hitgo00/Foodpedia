import Search from "../components/search";

export default function SearchPage() {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    >
      <Search />
    </div>
  );
}
