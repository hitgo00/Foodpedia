import Card from "../components/card";

export default function ProfilePage() {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    >
      <Card />
    </div>
  );
}
